# Widget Environment Variables (Example)

# API endpoint for the text chatbot
# Update this to your production API endpoint when deploying
VITE_API_ENDPOINT=http://localhost:3000/api/chat

# College/Institution ID
# Each institution should have their own unique ID
VITE_COLLEGE_ID=YOUR_COLLEGE_ID
